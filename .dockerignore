# Keep the Fly build context small and free of secrets / local state.
.git
.github
.venv
**/__pycache__
*.py[cod]
*.egg-info
.pytest_cache
.ruff_cache
.mypy_cache

# Runtime data + caches (mounted from a Fly volume at /app/data, never baked in)
data/
archive/

# Apartment frontend: node_modules + prior dist are rebuilt in the Docker stage
apartments/frontend/node_modules/
apartments/frontend/dist/
apartments/**/data/

# Secrets never go in the image — injected as Fly secrets at runtime
.env
.env.*
secrets/

.vscode
.idea
*.swp
.DS_Store
Thumbs.db
