# Only the deps the shorts project doesn't already install via its own pyproject.
# fastapi, uvicorn, httpx, pydantic, click, pyyaml, tenacity, python-dotenv,
# python-dateutil and pandas all come from shorts (directly or transitively);
# re-pinning them here only risks pip resolver conflicts (a hard pandas==3.0.2
# pin in particular clashed with shorts' yfinance/matplotlib).
#
# Unused-by-the-scanner deps removed to shrink the image: pyarrow, aiofiles,
# shapely, rapidfuzz (verified not imported anywhere in scanner/).
playwright>=1.40.0
playwright-stealth>=1.0.1
sqlite-utils>=3.35.0
python-telegram-bot>=20.0
pandas>=2.0
